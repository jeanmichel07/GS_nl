{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}
{% block stylesheets %}
{% endblock %}
{% block body %}
    <style>
        #id{
            text-align: center;
            margin-top: 65px;
            margin-left: 35%;
        }
        .btn{
            width: 150px;
        }
    </style>
    <div id="id" class="card border-primary mb-3" style="max-width: 20rem;">
        <div class="card-header"><b>GESTION DE STOCK NL_TECHNOLOGY</b></div>
        <div class="card-body">
            <a href="{{ path('client_new') }}" class="btn btn-outline-primary">Nouvelle vente</a><br><br>
            <a href="{{ path('read_produit') }}" class="btn btn-outline-primary">Les Stocks</a><br><br>
            <a href="{{ path('categorie_new') }}" class="btn btn-outline-primary">Nouvelle produit</a><br><br>
            <a href="{{ path('liste_vente') }}" class="btn btn-outline-primary">Les ventes</a>
        </div>
    </div>
{% endblock %}